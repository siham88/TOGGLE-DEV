
############################
#
# Block for cleaning the previous step
#
############################

$cleanerCommand = "rm -f $previousDir/*";

if (toolbox::run($cleanerCommand) == 1)
{
    toolbox::exportLog("INFOS : $0 : Removing intermediate results in $previousDir\n",1);
}

